// We are disabling the aggressive floating popup to prevent UI overlap 
// with the login screen. Push permissions will now be handled cleanly 
// via the Notification Dropdown inside Navbar.jsx.

const NotificationPrompt = () => {
  return null;
};

export default NotificationPrompt;